# Database Connection
# Supports PostgreSQL (Neon, Supabase, Vercel Postgres, or Local PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/meal_os?schema=public"

# App Timezone (Asia/Bangkok)
NEXT_PUBLIC_TIMEZONE="Asia/Bangkok"
